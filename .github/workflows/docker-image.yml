name: test

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    container: adnanmoussa/computational-workflows-homework
    
    steps:
      - name: Testingg
        uses: actions/checkout@v2

      - name: Run test
        run: |
          python3 -m pytest

